# SelfFace 功能规划建议

> 基于 2026-09-16 对 SelfFace 与 iFace 两侧源码的实测对比。
> 本文只回答一个问题：**在已完成安全加固 / Docker 部署 / 题库导入 / 限流之后，下一步该加什么，不该加什么。**

---

## 一、结论先行

按「对求职作品的价值」排序，**只有三件事值得投入**：

| 顺序 | 要加的功能 | 一句话理由 |
|---|---|---|
| 1 | **间隔重复调度（SRS）** | 你现在刷的是「临时记忆」，不是「面试记得住」；而且 **iFace 也没做**，做了就是差异化 |
| 2 | **JD 定向题单** | 直连你正在做的求职；iFace 的同类功能止于「报告」，你能做到「出题」 |
| 3 | **模拟面试闭环** | 复用你已有的简历分析产出；解决「自己给自己打分不客观」的根本问题 |

其余都是小补丁（收藏、笔记、打卡、导出），成本低但不加分。
**明确不建议照抄 iFace 的 Prompt 管理页和 AI 工具箱。**

---

## 二、最重要的发现：你的组卷算法有两个真实缺陷

这不是「缺功能」，是**现有逻辑在悄悄做错事**。证据在 `DailyTaskGenerator.pickQuestions()`：

```java
// 源码位置：backend/src/main/java/com/selfface/service/DailyTaskGenerator.java:83-89
List<Map<String, Object>> latest = practiceRecordMapper.latestMasteryPerQuestion(userId);
List<Map<String, Object>> reviewable = latest.stream()
        .filter(m -> PracticeService.asInt(m.get("mastery")) <= PracticeService.MASTERY_VAGUE) // <= 2
        .sorted(Comparator.comparingInt(m -> PracticeService.asInt(m.get("mastery"))))
        .limit(REVIEW_QUOTA) // 5
        .toList();
```

### 缺陷 1：答对的题会「永久消失」

过滤条件是 `mastery <= 2`，也就是 **mastery=3（掌握）的题从此再也不会进入复习池**。
只有题库被做空（第 3 步 fallback）才会重来，而那时是按 `orderByAsc("id")` 顺序**从头轮一遍**——这不是记忆曲线，是机械轮询。

后果：你在第 3 天「掌握」了一道 JVM 内存模型，第 30 天面试时它已经忘了，而系统认为你会。

### 缺陷 2：错题「每天必现」，没有间隔概念

一道 mastery=1 的题，今天做了、明天还在池子里、后天还在——**连续出现直到你答对**。
这是「刷到会为止」，短期能过面试，但**违背遗忘曲线**：真正有效的复习是「在快忘的时候再见到它」，间隔应该逐次拉长（1 天 → 3 天 → 7 天 → 21 天 → 60 天）。

### 缺陷 3：数据层没有时间维度，做不了间隔

`practice_record` 只有 `created_at`，**没有** `last_reviewed_at` / `next_review_at` / `interval_days` / `ease_factor`。
`latestMasteryPerQuestion()` 又只取每个问题的最新一条记录，**拿不到计算间隔所需的历史**。

> 补充一个小问题：`DAILY_SIZE = 10` 和 `REVIEW_QUOTA = 5` 都硬编码在类里，用户改不了。

### 结论

你现在的刷题引擎本质是「**错题优先 + 新题补充**」，缺的是**调度**。
补上它，这个项目才从「题库 + 记录」变成「记忆系统」。

---

## 三、建议 1（P0）：间隔重复调度

### 数据层

新增一张表（比改 `practice_record` 更干净——记录是明细，状态是聚合）：

```sql
CREATE TABLE IF NOT EXISTS review_state (
    id               BIGINT   NOT NULL AUTO_INCREMENT,
    user_id          BIGINT   NOT NULL,
    question_id      BIGINT   NOT NULL,
    last_reviewed_at DATETIME     DEFAULT NULL,
    next_review_at   DATE     NOT NULL COMMENT '到期日，<= 今天即进入复习队列',
    interval_days    INT      DEFAULT 0  COMMENT '当前复习间隔（天）',
    ease_factor      DECIMAL(4,2) DEFAULT 2.50 COMMENT 'SM-2 难度系数 1.30~2.80',
    review_count     INT      DEFAULT 0,
    lapse_count      INT      DEFAULT 0  COMMENT '遗忘次数',
    PRIMARY KEY (id),
    UNIQUE KEY uk_state_user_question (user_id, question_id),
    KEY idx_state_user_due (user_id, next_review_at)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT '复习调度状态';
```

### 算法（SM-2 简化版，够用且讲得清）

作答后更新状态：

| 自评 | interval_days | ease_factor | 说明 |
|---|---|---|---|
| 1 不会 | `1` | −0.20（下限 1.30），`lapse_count++` | 打回起点 |
| 2 模糊 | `max(1, round(interval × 1.2))` | −0.15 | 小幅前进 |
| 3 掌握 | `max(1, round((interval 或 1) × EF))` | +0.10（上限 2.80） | 正常倍增 |

`next_review_at = 今天 + interval_days`

**效果举例**：一道题连续答「掌握」，复习间隔会走 `1 → 3 → 8 → 22 → 60` 天。
回答面试官「你这个复习算法怎么设计的」时，这段能讲三分钟，而且是真算法不是 CRUD。

### 组卷改造

`pickQuestions()` 三步改为：

```
1. 到期复习题  ← WHERE next_review_at <= today
                  ORDER BY next_review_at ASC, ease_factor ASC, lapse_count DESC
                  LIMIT reviewQuota
   （越该复习的、越难记的、忘过越多次的，越优先）
2. 新题补充    ← 保持现有的 hot DESC + 分类均衡
3. 兜底        ← 题库小或全做完时，取「最近复习时间最久」的（而不是按 id 轮）
```

### 前端要加

- 错题本/题库页显示「下次复习：3 天后」
- 今日题单区分「新题 / 复习题」两个标签（现在混在一起）
- 设置页暴露 `dailySize`（10/15/20）

### 交付物

`SM2Scheduler`（纯函数，好测）+ 组卷改造 + 约 12 条单测（覆盖三种自评的间隔推进、EF 上下限、到期筛选）。

---

## 四、建议 2（P0）：JD 定向题单

### iFace 做到了哪一步（`src/pages/JdMatch.tsx`）

纯前端 prompt 工程：把简历 + JD 拼成一段话丢给 LLM，要它输出 **0-100 匹配分 + 匹配点 + 缺失关键词**。

**它的天花板很明显**：iFace 没有服务端题库（题库是静态 JSON），所以它**只能出一份报告，不能出题**。

### 你能做的闭环（iFace 做不到）

```
贴入 JD 文本
   ↓ ① LLM 抽取结构化关键词（要求返回 JSON：语言/框架/中间件/领域）
关键词集合（如 Spring Boot、MyBatis、Redis、消息队列、分布式事务）
   ↓ ② 在题库里匹配  ← 这里是技术含量所在
  · 起步：title / answer / tags 的 LIKE + MySQL 全文索引
  · 进阶：ES BM25 召回 → 向量召回 → RRF 融合 → Reranker 精排
     （正好复用你简历上的混合检索 + 精排链路，不是硬凑）
   ↓ ③ 生成「定向题单」
复用 daily_task，加 source 字段（DAILY / JD），生成一批针对该 JD 的题
   ↓ 输出
关键词命中率 + 缺失技能清单（题库里没有对应题的技术点）+ 一键开始刷
```

### 为什么值得做

1. **直连你正在做的事**：你在找 Java 后端实习，贴一份 JD 就知道自己差在哪、该刷什么——这是你自己会天天用的功能。
2. **有技术纵深**：匹配不是 `LIKE` 就完事，召回率/精确率、同义词（「JVM 调优」vs「GC 优化」）、中文分词都是可以展开讲的点。
3. **和 iFace 形成明确差异**：它止于报告，你止于题单。

### 交付物

`POST /api/jd/analyze`（抽关键词 + 匹配 + 生成题单）+ `JdMatch.vue` + 关键词抽取的 JSON schema 校验（LLM 输出必须 zod/Bean Validation 兜底，别直接信模型）。

---

## 五、建议 3（P1）：模拟面试闭环

### iFace 的做法（`src/pages/MockInterview.tsx`）

多轮对话结构做得挺完整：`turn` 分 interviewer/candidate，提问类型区分 `question` / `follow_up`（会追问），每轮有 score 评分和维度点评，开局可选 level（junior/mid/senior）和 type（technical/project/comprehensive）。

**它的短板**：用户要**手动选** level 和 type，面试官不知道你的简历。

### 你的天然优势

`ResumeService` 已经在做「解析简历 → 预测面试问题」了。**模拟面试的题源你已经有了**，缺的只是把「静态问题列表」变成「多轮对话」。

### 建议形态

```
选一份已解析的简历  →  LLM 基于简历生成开场问题
   ↓
面试官提问 → 用户作答 → LLM 追问（戳细节："你说用了 Redis，缓存穿透怎么处理的？"）
   ↓ 循环 N 轮，每轮打分 + 指出漏洞
   ↓
面试报告：各维度得分 / 答得最差的 3 个问题 / 建议复习的题目（关联回题库）
```

### 为什么值得做

**你现在最大的方法论问题是：mastery 靠自评。**
自己觉得「掌握了」和真的讲得出来，差距很大。LLM 追问能戳穿「背了但没懂」——这是模拟面试的核心价值，不是「多一个页面」。

### 交付物

`interview_session` + `interview_turn` 两张表，`MockInterviewController`，对话页（复用 Resume.vue 的轮询模式处理流式/异步），最终报告可导出 Markdown。

---

## 六、建议 4（P1/P2）：低成本补丁

都是几小时能完成的，可以攒一批一起做：

| 功能 | 做法 | 成本 |
|---|---|---|
| **题单容量可配** | 设置表加 `daily_size`，替换 `DailyTaskGenerator.DAILY_SIZE` 常量 | 1 小时 |
| **题目收藏** | `user_question_mark(user_id, question_id, type)`，type 区分收藏/标记 | 3 小时 |
| **答题笔记** | 同上表加 `note` 字段；题库详情弹窗已有，加个输入框 | 3 小时 |
| **连续打卡 streak** | Dashboard 已有近 14 天趋势数据，加一个「连续 N 天」的 SQL 计算 | 1 小时 |
| **错题本导出** | `GET /api/practice/wrong-book/export?format=md`，面试前打印用 | 2 小时 |
| **目标日期倒推** | 设置「我 11 月要面试」→ 算出每天该刷多少题、进度条 | 4 小时 |

---

## 七、明确不做的三件事

### 1. Prompt 管理页（iFace 用了 2461 行做这个）

iFace 让用户在前端手搓 prompt 模板。**照抄会犯两个错**：

- **把质量责任推给用户**：用户不知道怎么写 prompt 才能让模型输出结构化结果，做出来大概率是「能用但很难用」的功能。
- **丢掉了服务端的最大优势**：你的 prompt 在代码里，可以随版本迭代、可以做 A/B、可以按模型适配。**放在数据库里版本化是对的，但不需要给用户编辑界面。**

如果要让 prompt 可配，正确做法是：后端存 `prompt_template` 表（含版本号 + 生效开关），管理入口做成你自己的运维接口，而不是产品页面。

### 2. AI 工具箱（AITool）

一个「什么都能问」的通用聊天框。**它会稀释产品焦点**——面试产品的用户不需要通用 AI 助手，需要「针对我的目标岗位，告诉我该练什么」。功能宽度不是加分项，**纵深才是**。

### 3. 巨石文件（iFace 的 `QuestionDetail.tsx` 6689 行）

反面教材。你的前端最大文件是 `Resume.vue` 630 行，这个体量很健康。**新增功能时保持这个纪律**：一个页面超过 800 行就该拆组件。

---

## 八、优先级总表

| 优先级 | 功能 | 价值 | 预估工作量 |
|---|---|---|---|
| **P0** | 间隔重复调度（SM-2） | 算法护城河 + 简历亮点 + iFace 没有 | 1~2 天 |
| **P0** | JD 定向题单 | 直连求职 + 可复用 RAG 技术栈 | 2~3 天 |
| **P1** | 模拟面试闭环 | 复用简历分析 + 解决自评不客观 | 3~4 天 |
| **P1** | 题单容量可配 | 基础体验 | 1 小时 |
| **P2** | 收藏 / 笔记 | 刷题刚需 | 半天 |
| **P2** | streak 打卡 | 激励，极低成本 | 1 小时 |
| **P2** | 错题本导出 | 面试前打印 | 2 小时 |
| **P2** | 目标日期倒推 | 计划感 | 半天 |

---

## 九、一句话总结

iFace 的 11 个页面里，**有 4 个（模拟面试、JD 匹配、Prompt 管理、AI 工具箱）是可以选择性地吸收经验的**，
但真正值得你投入的是**它也没做的那件事——让题目在正确的时间重新出现**。

补齐调度算法，这个项目就从「一个能刷题的网站」变成「一个懂记忆规律的备考系统」。
前者是 CRUD 练习，后者是**能讲出设计取舍的系统**——面试官会记住后者。
