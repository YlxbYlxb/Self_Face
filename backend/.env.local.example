# 复制成 .env.local 后填写，该文件不会被提交。
#   cp .env.local.example .env.local
#
# 只需填数据库口令，其余留空即用 application.yml 里的默认值。

# 数据库口令（必填）
DB_PASSWORD=

# 以下可选
# DB_USERNAME=root
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=selfface

# 覆盖服务端口（默认 8081）
# SERVER_PORT=8081
