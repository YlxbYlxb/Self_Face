# 项目对比分析：iFace vs SelfFace

> 对比时间：2026-09-16
> iFace 版本：v1.4.2（github.com/dogxii/iFace，MIT）
> SelfFace 版本：v1.0.0（本地项目）

---

## 一句话结论

**iFace 是「把一个功能做到极深」的成熟单品，SelfFace 是「把一条完整链路跑通」的工程练手项目。**

两者定位不同、胜负项不同：iFace 赢在题库厚度、功能深度和工程纪律；SelfFace 赢在有真正的服务端架构、测试覆盖和异步任务设计。**你现在缺的不是技术能力，是内容厚度和一个部署出口。**

---

## 一、核心指标对照

| 维度 | iFace | SelfFace | 谁强 |
|---|---|---|---|
| 定位 | 本地刷题工具，零后端 | 多用户学习平台，有服务端 | — |
| 技术栈 | React 19 + TS + Vite 7 + Tailwind 4 | Vue 3 + Element Plus + SpringBoot 3.3 + MySQL | — |
| 代码规模 | 35,614 行 TS/TSX（48 文件） | 5,227 行 Java+Vue（58 文件） | iFace |
| 最大单文件 | QuestionDetail.tsx **6,689 行** | PracticeService.java 12.8KB（约 350 行） | SelfFace |
| 题库 | **1,222 题** / 29 模块 / 4 大方向 | 112 题 / 11 分类 / Java 单方向 | iFace |
| 功能页面 | 11 个 | 7 个 | iFace |
| 数据存储 | IndexedDB 本地 + Gist 云同步 | MySQL 服务端 | 各有取舍 |
| 用户体系 | 无注册（可选 GitHub 登录备份） | 注册登录 + JWT，多用户隔离 | SelfFace |
| AI 集成 | 浏览器直连：AI 教练 / 模拟面试 / JD 匹配 | 服务端代理：简历分析 Agent（异步） | iFace 功能宽，prep 工程设计稳 |
| 单元测试 | **0** | 15 个 | SelfFace |
| 质量脚本 | 15 个 check 脚本 + 11 项发布门禁 | 3 个 smoke / verify 脚本 | iFace |
| Lint / CI | Biome（强制） | 无 | iFace |
| 部署状态 | Vercel 线上运行，有真实用户 | 仅本地 `run.cmd` | iFace |
| 许可证 | MIT，开源 | 未开源 | — |

---

## 二、架构路线：两条完全不同的路

### iFace —— 纯前端 + 本地优先（Local-First）

```
浏览器 ── IndexedDB（题库/进度/对话）
   └── 可选 GitHub Gist 同步（跨设备备份）
   └── 直连 LLM API（Key 存本地）
   └── Vercel 托管静态资源 + 一个 auth 接口
```

- **优点**：零服务器成本、零运维、打开即用、离线可刷题（PWA）、数据主权在用户手里。
- **代价**：多设备同步靠 Gist 这种「土办法」，机制复杂且脆弱；没有服务端就做不了多用户社区、排行榜、题目标注协作；重业务逻辑全压在浏览器里，所以出现了 6,689 行的巨型组件。

### SelfFace —— 前后端分离 + 服务端权威

```
Vue 3 (5273) ──HTTP/JWT──> SpringBoot 3.3 (8081) ──> MySQL
                                  └── 线程池异步任务（简历分析）
                                  └── 服务端代理 LLM（Key 存 DB）
```

- **优点**：数据模型清晰（7 张表）、多用户天然隔离、跨设备零成本、LLM Key 不下发到浏览器、异步任务链路完整（PENDING→RUNNING→SUCCESS/FAILED + 启动恢复）。
- **代价**：有部署和运维成本，目前**没有部署方案**（这是你的 P0）。

> **判断**：作为求职作品，服务端这条线更有说服力 —— 面试官更想看 MyBatis 映射、事务边界、并发守卫、线程池配置这些。iFace 那套「纯前端」在简历上反而不好讲。**你的架构选型是对的。**

---

## 三、功能覆盖度对比

| 能力 | iFace | SelfFace |
|---|---|---|
| 题库浏览 / 分类筛选 | ✅ | ✅ |
| 每日刷题 / 组卷 | ✅（Practice） | ✅（每日组卷算法，按掌握度加权抽题） |
| 掌握度记录 | ✅ | ✅（1 不会 / 2 模糊 / 3 掌握） |
| 错题本 | ✅（WeakPoints 弱项分析） | ✅（WrongBook） |
| 统计数据看板 | ✅（Dashboard 1,964 行） | ✅（SQL 聚合，已优化） |
| 题库导入（自定义） | ✅（ImportPage 支持 JSON） | ⚠️ 仅内置 seed，无导入 UI |
| AI 辅助答疑 | ✅（AI 教练，任意题追问） | ❌ |
| 模拟面试 | ✅（MockInterview 1,606 行） | ❌ |
| JD 匹配分析 | ✅（JdMatch 771 行） | ❌ |
| Prompt 模板管理 | ✅（PromptPage 2,461 行） | ❌ |
| 简历解析 → 预测问题 | ❌ | ✅（你的差异化功能） |
| 多 LLM 供应商配置 | 单套配置 | ✅（DeepSeek/通义/Kimi/Ollama 可切换 + 连通性自检） |
| PWA 离线 | ✅ | ❌ |
| 数据导入导出 / 备份 | ✅（localBackup 460 行） | ❌ |

**读法**：iFace 的宽度是 3 年的迭代堆出来的（11 个页面里有 4 个是你的项目完全没有的）。但 **「简历解析 → 预测面试问题」是它没有的**，这恰好是最贴近求职者真实痛点的功能，也是你可以在面试里讲出差异化的点。

---

## 四、代码质量：各自的问题清单

### iFace 的问题（详见 `iFace-代码审查建议.md`）

| 问题 | 严重度 | 说明 |
|---|---|---|
| QuestionDetail.tsx 6,689 行 | 高 | 单文件塞了详情、AI 对话、笔记、掌握度、标签等所有逻辑，改一处怕碰全局 |
| PromptPage 2,461 行 / QuestionList 2,155 行 | 高 | 同样的巨石问题，说明缺少组件拆分纪律 |
| 零单元测试 | 高 | AGENTS.md 明确写「There is no dedicated test runner configured」，全靠手工验证 + check 脚本 |
| PWA 误缓存 learning-checks | 中 | 2.2MB 的构建产物被 Service Worker 缓存，更新后用户拿到旧数据 |
| gistSync 静默丢数据 | 中 | 1,213 行的同步逻辑，自定义题在冲突合并时会无声丢失 |
| 主包 1,179KB / gzip 378KB | 中 | 首屏偏重 |
| 题库 json 未做 schema 校验入库 | 低 | 导入的是「不可信输入」，仅靠 `check:questions` 事后检查 |

**但要承认**：它有 15 个 check 脚本 + 11 项发布门禁 + Biome 强制规范 + 版本同步校验 + 发布审计文档（`docs/RELEASE_AUDIT_1.0.md`）。**这套「流程纪律」比很多商业项目都齐全，值得你抄。**

### SelfFace 的问题（详见 `优化报告.md`）

| 问题 | 严重度 | 说明 |
|---|---|---|
| 无部署路径 | **P0** | 没有 Dockerfile、没有 prod profile、没有 Nginx 配置，只能 `run.cmd` 本地跑 |
| JWT secret 用默认值 | **P0** | 上线前必须换成环境变量注入 |
| LLM API Key 明文存库 | **P0** | 多用户场景下必须加密（Jasypt / KMS） |
| Element Plus 全量引入 | P1 | 主包 1,179.7KB / gzip 378KB，和 iFace 踩同一个坑 |
| 题库检索用 LIKE | P1 | 无相关性排序，题量上千后要换 ES 或加 embedding 召回 |
| 无速率限制 | P1 | 只做了「同用户单任务」并发守卫，没有时间窗口限流 |
| 无外键 / 无级联 | P2 | 删题不清理刷题记录 |
| 无登录防爆破 | P2 | 无失败次数锁定 |
| 无前端错误边界 | P2 | — |

**关键差异**：你的问题**集中在「上线前的安全与部署」**（都是可以半天补完的工程活），iFace 的问题**集中在「代码结构的长期债」**（重构成本很高）。**从这个角度说，你的项目底子更干净。**

---

## 五、iFace 值得你直接抄的三件事

### 1. 质量门禁脚本化

它把「发布前该检查什么」写成了 15 个可执行脚本：
`checkVersionSync`（版本号一致性）、`checkDocsAssets`（文档引用的图片是否存在）、`checkQuestions`（题库结构校验）、`checkQualityGate`（质量门禁）、`checkPWAAssets`…

**你的动作**：把现有的 3 个验证脚本扩成一套 `scripts/`，至少加三个 ——
- `check_seed.py`：校验 seed JSON 字段完整性（题干/答案/分类/难度非空、分类在 schema 内）
- `check_api_contract.py`：拉一遍所有接口，比对字段是否和 README 的接口表一致
- `check_security.py`：扫描配置，若 `jwt.secret` 是默认值或 `llm key` 未加密则拒绝启动

这既是工程习惯，也是面试时的加分项（"我怎么保证质量？我自己写了门禁脚本"）。

### 2. 每一个「功能」都配一份文档

iFace 有 `docs/ROADMAP.md`、`RELEASE_NOTES`、`RELEASE_AUDIT`、`SMOKE_TEST.md` + 实测结果 JSON。它的**发布是有仪式感的**。

**你的动作**：README 已经写得不错了，再加一份 `CHANGELOG.md` 和 `docs/smoke-result-latest.md`，把每次 smoke 测试的结果落盘（现在只存在于终端输出里）。

### 3. 题库「导入」而不是「写死」

iFace 有专门的 ImportPage，用户的 JSON 能直接进库，内置题库也是同一条链路加载的。**这让内容扩展的成本降到零** —— 所以它能从几十题堆到 1,222 题。

**你的动作**：这是你最大的短板。现在 112 题全部写死在 `backend/src/main/resources/seed/*.json`，加题要重新打包后端。应该：
- 加一个 `POST /api/questions/import`（管理员或任意用户），沿用同一份实体
- 前端加导入页，支持粘贴 JSON / 上传文件
- 保留 seed 作为首次启动的初始化数据
- 这样题库能从 112 涨到 1000+，且不需要你手写代码

---

## 六、iFace 的坑，你务必避开

1. **别让单文件超过 800 行。** QuestionDetail 6,689 行的教训。你的 `PracticeService.java` 350 行、`ResumeService.java` 约 200 行，现在的粒度是健康的 —— 保持住，下次再加功能时先想"这是不是该拆了"。
2. **别用 PWA / Service Worker 缓存构建产物，除非你想清楚缓存失效策略。** iFace 在这里翻过车。
3. **别自己手写同步逻辑。** gistSync 1,213 行还在丢数据。你有服务端，跨设备是免费的 —— 这是你的架构红利，别放弃。
4. **别把 AI Key 放浏览器。** iFace 因为纯前端只能这样（虽然写进 README 说"仅存本地"），但你已经走服务端了，加密存储是必须的。
5. **构建主包别全量引入 UI 库。** 你们两个都踩了 —— 你现在可以和面试官说"我发现了这个问题并准备改用按需引入"，这就是有意识的工程师。

---

## 七、你的项目的相对优势（面试时可以讲的）

1. **完整的异步任务链路**：`PENDING → RUNNING → SUCCESS/FAILED` + 线程池 + 启动时中断任务恢复 + 同用户并发守卫。iFace 作为纯前端没有这类问题，也就没有这类经验。**这是你简历上最硬的一块。**
2. **有服务端的分层架构**：Controller / Service / Mapper / Entity 清晰，MyBatis-Plus 映射、SQL 聚合优化（11 次 COUNT → 1 次 GROUP BY）、`MAX(seq)+1` 修并发竞态。这些是纯前端项目讲不出来的。
3. **有单元测试**：15 个用例覆盖组卷、掌握度、LLM 配置。iFace 是 0。**"我写了测试"这句话在面试里很值钱。**
4. **多 LLM 供应商抽象**：OpenAI 协议兼容 + 连通性自检，可切 DeepSeek/通义/Kimi/Ollama。这是 RAG / Agent 方向的直接经验积累，和你的 Medical RAG、Mewcode 项目形成呼应。
5. **性能优化有实测数据**：上传从「数十秒阻塞」到「24ms 返回」、N+1 消除、全表物化改定点查询 —— **有前后对比数字的优化故事，面试官最爱听。**

---

## 八、行动优先级建议

| 优先级 | 事项 | 理由 |
|---|---|---|
| **P0** | 写 Dockerfile + docker-compose，跑通 `docker compose up` 一键启动 | 没有部署出口的项目在简历上是减分项；这也是你「Docker / Linux」技能栈的证明 |
| **P0** | JWT secret 改环境变量、API Key 加密存储 | 上线安全底线 |
| **P1** | 加题库导入接口 + 前端导入页 | 解开内容瓶颈，才有可能从 112 涨到 1000+ |
| **P1** | Element Plus 改按需引入，主包砍到 400KB 以下 | 有明确的优化前后数据可讲 |
| **P1** | 限流（Bucket4j / 拦截器）+ 登录失败锁定 | 服务端项目的标配 |
| **P2** | 补 `CHANGELOG.md`、`docs/` 目录、质量门禁脚本 | 抄 iFace 的流程纪律 |
| **P2** | Git 化（现在是裸目录，没有版本历史） | 作品集需要 commit 记录来体现迭代过程 |

---

## 九、总结

| | iFace | SelfFace |
|---|---|---|
| 它证明了什么 | 一个功能做深 + 内容做厚，可以没有后端也活得很好 | 一条完整的服务端链路是可以独立跑通的 |
| 你该学它什么 | **工程纪律（门禁脚本、发布文档）** 和 **内容可扩展性（导入机制）** | — |
| 你该超过它什么 | 你已经超过的部分：测试、异步设计、服务端分层 | — |
| 你该避开它什么 | 巨石文件、手写同步、UI 全量引入、无测试 | — |

**最终判断**：这两个项目不是竞争关系，而是**互补的参照系**。iFace 告诉你「内容厚度和流程纪律长什么样」，你的项目证明了「你有服务端和异步的工程能力」。

**当前最该做的一件事**：把 P0 的两项（部署 + 安全配置）补完。补完之后，SelfFace 就从「一个能跑的项目」变成「一个能给别人用的项目」—— 这个跨度，比把题库从 112 加到 200 题有价值得多。

---

*附：iFace 的详细代码问题清单见 `iFace-代码审查建议.md`；SelfFace 自身的优化记录见 `优化报告.md`。*
