# SelfFace · 优化报告

> 针对 `E:\SelfFace`（本轮新建的项目）。所有结论都经过实测：
> `mvn test` 15 项、`smoke_test.py` 22 项、`verify_async_resume.py`、`verify_inflight_guard.py` 全绿，
> 并在真库上跑通了每一条新写的 SQL。

## 一句话结论

功能是完整的，但**写法上留了 6 处会在真实使用中出问题的地方**——其中 3 处是「用户量稍微上来就暴露」的性能问题，
2 处是并发下会报 500 或丢数据的正确性问题，1 处是架构问题（把模型推理塞在 HTTP 请求里）。
这 6 处本轮已经全部修掉并验证；剩下的是「上线前必须做」和「作品打磨」两类，列在后面。

---

## 一、本轮已修（附实测证据）

### 1. 简历分析从「同步阻塞」改成「后台任务 + 轮询」⭐ 最重要

**问题**：`LlmClient` 单次调用超时上限 600 秒，而分析要串两次调用（画像 + 出题），
理论上最长 1200 秒；但 MVC 的 `request-timeout` 只有 300 秒。
也就是说 **请求早就超时了，后台还在继续烧 token**，用户看到的是超时错误，账单是真的。
另一个被忽略的细节：`resume_analysis` 表里本来就有 `status` 字段（PENDING/RUNNING/SUCCESS/FAILED），
说明表结构一开始就是按异步设计的，只是实现没用上。

**改法**：

```
POST /api/resume/analyze → 校验配置 + 抽文本 + 写 PENDING 记录 → 立刻返回 {id, status}
                           ↓ resumeExecutor（core 2 / max 4 / queue 16，AbortPolicy）
                          RUNNING → 调模型 → SUCCESS / FAILED（原因写 error_msg）
GET  /api/resume/{id}    → 前端轮询直到终态（间隔 1.5s 起，退避到 5s）
```

**实测**：上传接口从「等几十秒」变成 **24 ms**；状态流转 `RUNNING → FAILED` 正确；
失败原因落库为「无法连接模型服务（ConnectException）」。

代码里几个刻意的细节（都是容易踩的坑）：

| 细节 | 为什么这么做 |
|---|---|
| 提交方法**不加** `@Transactional` | 任务记录必须先真正提交，后台线程按 id 才查得到 |
| 异步方法放在独立 bean `ResumeAnalysisJob` | `@Async` 依赖代理，同类内自调用不生效 |
| 状态更新**不加**事务 | 长事务会把 RUNNING 锁到结束，前端永远看不到中间态 |
| 启动时清理残留任务 `ResumeTaskRecovery` | 进程被杀后 PENDING/RUNNING 会永远卡住：前端一直转圈 + 并发守卫永久拒绝新提交 |

并发守卫也验证过了：注入一条 RUNNING 记录后上传被拦住，且不产生新任务记录。

### 2. 热路径上的三处查询浪费

| 位置 | 改前 | 改后 |
|---|---|---|
| `PracticeService.decorate()` | **每次** `/practice/today` 都全量拉 `latestMasteryPerQuestion`（行数 = 答过的题目数），只为算一个 `isReview` 布尔 | `masteryOfQuestions(userId, 当天10个id)`，走索引定点查 |
| `PracticeService.stats()` | 把用户全部作答行拉进 Java，再 `stream().filter().count()` | `masterySummary()` 一条 SQL 聚合出 answered / mastered / weak |
| `QuestionService.categoriesWithCount()` | 先查分类列表，再对**每个分类**单独 `COUNT`（11 次往返） | `countGroupByCategory()` 一次 `GROUP BY` |
| `PracticeService.wrongBook()` | 全量拉取后在 Java 里过滤 + 排序 | `latestFiltered()` 把过滤与排序下沉到 SQL |

按每天 10 题算，一年就是 3650 条作答记录。改前每次打开首页都要把这些行全部从 MySQL 搬到 JVM 再丢掉；
改后是常数级。**库小时看不出差别，这正是它危险的地方。**

### 3. 并发生成今日题单会报 500

**问题**：`todayTasks` 先查为空、再逐条 `insert`，两个标签页同时打开，
后到的那个会撞上唯一键 `uk_daily_user_date_question`，异常直接抛到用户面前。

**改法**：组卷逻辑抽成独立的 `DailyTaskGenerator`（`@Transactional`），
用 `DailyTaskMapper.batchInsertIgnore()` 一条 `INSERT IGNORE` 批量写 10 行——
唯一键本身就是幂等保证，并发下既不报错也不重复，顺带把 10 次往返压成 1 次。
读取端刻意**不加事务**：生成后必须在新快照里重读，否则 REPEATABLE READ 下会一直读到空。

### 4. `appendToToday` 的序号算错

原代码用 `selectCount(...)` 的结果当新序号。题目一旦被删过，`COUNT` 就和真实序号错位，
新追加的题会插到中间去。改成 `MAX(seq) + 1`。

> 遗留：两个人**同时**追加不同的题，仍可能读到同一个 MAX 得到相同 seq。不影响正确性（只影响展示顺序，无唯一约束），
> 真要做到严格递增得靠 `SELECT ... FOR UPDATE` 或应用层串行化。已记在已知限制里。

### 5. 错误提示里出现 `null`

这条是**跑验证时才发现的**：连接被拒时 `ConnectException.getMessage()` 返回 null，
于是用户看到「无法连接模型服务：null」。已改成回退到异常类名：
「无法连接模型服务（ConnectException）」。

### 6. 补上测试（原本是 0）

`pom.xml` 里一直有 `spring-boot-starter-test`，但 `src/test` 是空的。新增 15 项单元测试，
全部钉在「改坏了不报错、但线上出问题」的地方：

- `PracticeServiceTest`（7 项）：热路径不再全量拉取、追加用 MAX(seq)、统计走 SQL 聚合、错题本过滤下沉、
  连续打卡的 6 种边界（含「今天没刷但昨天刷了算连续」）
- `DailyTaskGeneratorTest`（3 项）：复习题优先级与 5 道上限、题库为空抛可读异常、
  新题按分类轮转（断言 `[1,5,2,6,3,7,4,8]` 这种交替模式）
- `LlmSettingServiceTest`（5 项）：只改模型时不清空 Key、回传的掩码串不会被当成新 Key 存回去、
  温度/超时被夹到安全区间

冒烟测试也从 20 项加到 22 项（补了「未配置 Key 时上传立刻失败」和「被拒绝的上传不留垃圾记录」）。

---

## 二、验证结果

```
mvn test                      Tests run: 15, Failures: 0, Errors: 0
smoke_test.py                 通过 22 项，失败 0 项
verify_async_resume.py        上传 24ms / 状态流转 RUNNING→FAILED / 失败落库 / 终态后可重提
verify_inflight_guard.py      已存在进行中任务时上传被拒，且不新建任务
ResumeTaskRecovery            启动日志：启动时清理了 2 条中断的简历分析任务
```

真库上逐条验证过的新 SQL：`countGroupByCategory`、`maxSeq`、`masteryOfQuestions`、
`latestFiltered`、`masterySummary`、`batchInsertIgnore`。

---

## 三、还没做，按优先级排

### P0 — 上线前必须做

**1. 没有任何部署路径。** 目前只有 `run.cmd`，靠本机 `mvn spring-boot:run` + `npm run dev` 跑。
作为一个要放进简历、面试官会想点开看的作品，这是最大的短板。最小可行方案：

```dockerfile
# backend/Dockerfile —— 两阶段，产物镜像约 200MB
FROM maven:3.9-eclipse-temurin-21 AS build
WORKDIR /app
COPY pom.xml . && RUN mvn -B dependency:go-offline
COPY src ./src && RUN mvn -B -DskipTests package

FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
COPY --from=build /app/target/*.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java","-Xms256m","-Xmx512m","-jar","app.jar"]
```

配套还要三件事：`application-prod.yml`（关掉 SQL 初始化日志、把 JWT secret 和 DB 密码改成必填无默认值）、
Nginx 反代 `dist/` 静态文件并把 `/api` 转给后端、`RESUME_EXECUTOR` 线程池参数按机器规格调。

**2. JWT secret 有内置默认值。** `application.yml:46`
`secret: ${JWT_SECRET:selfface-dev-secret-please-change-it-2026}` —— 上线不换等于没有鉴权。
建议改成 `${JWT_SECRET}`（无默认值，启动即失败）并加长度校验。

**3. API Key 明文入库。** `llm_setting.api_key` 直接存明文。个人用没问题，
但只要项目开源、或者数据库有备份外泄风险，别人的 Key 就泄露了。
建议用 `AES-GCM` + 环境变量里的主密钥加密，写一个 MyBatis `TypeHandler` 做透明加解密，
存量数据写个一次性迁移把明文行补成密文。

### P1 — 明显影响体验

**4. Element Plus 全量引入，主包 378 KB gzip。** 实测：

```
dist/assets/index-*.js    1179.7 KB（gzip 378 KB）
dist/assets/markdown-*.js  102.6 KB
```

对一个 8 页的应用来说太重了，首屏白屏时间可见。改按需引入能砍到 120–150 KB：

```bash
npm i -D unplugin-vue-components unplugin-auto-import
```

```js
// vite.config.js
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

plugins: [
  vue(),
  AutoImport({ resolvers: [ElementPlusResolver()] }),
  Components({ resolvers: [ElementPlusResolver()] })
]
```

同时把 `main.js` 里的全量注册删掉。注意两点：`ElMessage`/`ElMessageBox` 是函数式调用，
`AutoImport` 能处理；但**图标不要再全量注册**（`main.js` 现在把 2000 多个图标全注册了一遍），
改成用哪个 import 哪个。

**5. 检索没有相关性排序。** `QuestionService.page()` 和 `search()` 用的都是
`like("title", kw).or().like("tags", kw)`，结果是按 `hot` 排的，跟查询词的相关度无关。
搜「索引」第一屏可能全是别的题。两个层次的改法：

- **轻量**：加 ngram 全文索引，用 `MATCH ... AGAINST` 的 `score` 排序。
  ```sql
  ALTER TABLE question ADD FULLTEXT INDEX ft_question_title_tags (title, tags) WITH PARSER ngram;
  ```
  注意单字查询（ngram 默认 token 长度 2）要回退到 `LIKE`，两套逻辑并存。
- **进阶**：题库上千后上 ES 或加一层 embedding 召回 —— 这正好是你项目 1 的强项，
  做成「题库向量化 + BGE-m3 召回 + Reranker 精排」，是简历上能讲的一条完整链路。

**6. 没有速率限制和成本可见性。** 现在只防了「同一用户同时一个任务」。
简历分析的 token 数已经落库了（`prompt_tokens` / `completion_tokens`），
但没做成「今日已消耗 X tokens」的展示，用户对自己的花费没有感知。
建议加一个仪表盘卡片 + 每用户每日配额。用 Redis 的 `INCR` + `EXPIRE` 十几行就能做出滑动窗口限流。

### P2 — 打磨项

**7. 表间没有外键与级联。** 删题目不会清理 `daily_task` / `practice_record` 里的孤儿行。
要么加 `ON DELETE CASCADE`，要么在删除题目的接口里显式清理（推荐后者，可控且不锁表）。

**8. `submit` 与「今日进度」的语义不一致。** 从题库页直接提交作答时，
`daily_task` 里没有对应行，这次作答**不计入今日进度**，但会计入总统计。
用户会觉得「我明明做了 3 题，进度还是 0/10」。要么提交时自动补进今日题单，要么在 UI 上说清这是「自由练习」。

**9. 登录接口没有防爆破。** 没有失败次数限制、没有图形验证码、JWT 有效期 7 天且无法吊销。
个人项目可以接受，但值得在 README 里写明。

**10. 前端没有错误边界。** 单个页面组件抛错会整页白屏（Vue 的 `errorCaptured` 或 `app.config.errorHandler` 能兜住）。
另外 `Dashboard.vue` 414 行、`Resume.vue` 588 行，还没到必须拆的程度，但已经开始混职责了
（`Resume.vue` 里数据请求、轮询、模板渲染全在一起），下次加功能前建议把轮询逻辑抽成 `useResumeAnalysis()` composable。

**11. Hikari 连接池 10 / 异步池 4 的组合需要压测。** 4 个后台任务同时跑时，
每个任务的分析流程会持有连接；`maximum-pool-size: 10` 在多用户场景下可能被吃满，
导致普通查询排队等连接。上线前建议用 `wrk`/`ab` 压一轮看看。

---

## 四、如果只做三件事

1. **补部署**（Dockerfile + compose + Nginx + prod profile）—— 作品要能被看到，这是当前唯一的功能性缺口
2. **Element Plus 改按需引入** —— 实测能省 200+ KB gzip，改动小、收益直接
3. **检索加 ngram 全文索引 + 按相关度排序** —— 题库一上千就没法用了，而且这条链路是简历上的加分项

---

## 五、两个值得记住的教训

1. **同一文件不要并行编辑。** 这轮有两次「两个 Edit 同时提交到同一个文件」，后写的那个基于旧快照，
   把前一个改动覆盖掉了（一次吞掉 `import`，一次把 `toView(r)` 改回 `toView(r, 0)`）。
   表现是编译报错，但排查要花时间——老老实实串行改。

2. **bash 的 `UID` 是只读变量。** 写 `UID=$(mysql ...)` 会静默失效（只报一行 readonly 警告），
   后续 `$UID` 展开成 shell 的真实 uid，导致我往错误的 `user_id` 上插了一条测试记录，
   测出来的结论是假阴性。脚本里别用 `UID`、`PID`、`PPID`、`RANDOM` 这些保留名。
