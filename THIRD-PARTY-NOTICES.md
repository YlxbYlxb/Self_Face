# 第三方内容来源与许可

本项目的**代码**版权归项目作者所有；**题库内容**分两部分：一部分为项目自有题目，
另一部分提取自下列开源项目。按相应许可证的要求，在此声明来源与改动。

---

## 1. JavaGuide

| 项 | 内容 |
|---|---|
| 来源 | <https://github.com/Snailclimb/JavaGuide> |
| 作者 | Guide（Snailclimb） |
| 许可证 | Apache License 2.0 |
| 使用位置 | `backend/src/main/resources/seed/04-javaguide.json`（1182 道题） |
| 提取脚本 | `scripts/import-javaguide.py` |

### 改动说明

依据 Apache-2.0 第 4 条（保留声明、标明修改），对原文档做了以下改动：

1. **结构重组**：原文档是连贯的技术文章（`##` 章节 + `###` 小节），
   本项目按标题切分为「题干 + 参考答案」的问答对。
2. **移除站内相对链接**：原文里指向 JavaGuide 站内其他文档的相对链接，
   在本站会 404，已替换为纯文本（外部链接保留）。
3. **统一字段**：补充 `category`（分类）、`difficulty`（按答案长度归一到 1~3）、
   `hot`（高频标记）、`tags`（取自原文所属章节名）等本项目自定义字段。
4. **去重**：与项目自有题目按「归一化精确匹配 + 文本相似度 ≥ 0.82」两级去重。
5. **节选**：超过 6000 字的答案在段落边界截断，并在末尾注明「此处为节选」。
6. **剔除**：与问答无关的结构性标题（目录、前言、参考资料、推荐阅读等），
   以及 Markdown 中的图片与徽章（图片托管在外部 CDN，引用会导致失效）。

> 提取规则全部固化在 `scripts/import-javaguide.py` 中，可用同一份脚本复现，
> 不做人工二次改写。

### 许可证全文

Apache License 2.0：<https://www.apache.org/licenses/LICENSE-2.0>

---

## 使用建议

若你要把本项目用于**商业用途**，请自行评估上述内容的合规性：

- 本项目自有题目（`01`~`03`）不受第三方许可约束。
- JavaGuide 部分遵循 Apache-2.0，可商用，但必须**保留本声明与出处**，
  且不得使用原作者名义做背书。
- 本项目的提取脚本仅做格式转换与筛选，**不改变原内容的著作权归属**。

如果仓库中出现了你不希望被引用的内容，欢迎提 issue，会立即移除。
