# Java / Maven
backend/target/
backend/*.log
*.class

# Node / Vite
frontend/node_modules/
frontend/dist/
frontend/*.log

# 运行时数据
*.log
logs/

# 环境变量（含 JWT / 加密密钥，绝不入库）
.env
.env.*
!.env.example
backend/.env.local
backend/.env.*
!backend/.env.local.example
frontend/.env.local
frontend/.env.*
!frontend/.env.local.example
!frontend/.env.production.example

# 本机私有启动配置（含数据库口令，绝不入库）
run.local.cmd
*.local.cmd
!*.local.cmd.example

# Vite 生成的临时配置快照
frontend/vite.config.js.timestamp-*

# 题库导入用的原始素材（PDF 等，体积大且多为第三方版权内容，不入库）
pdf-inbox/
scripts/.extract-cache/
*.pdf

# 第三方题库数据：来源方声明禁止转载，仅供本地个人学习使用，不入公共仓库
# 由 scripts/import-interview-pdf.py 生成，本地重新跑脚本即可复现
backend/src/main/resources/seed/05-interview-pdf.json

# Python 编译缓存
__pycache__/
*.pyc

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws
.settings/
.project
.classpath

# OS
.DS_Store
Thumbs.db
desktop.ini
