# 对比复评：加完 SRS / JD 定向题单 / 模拟面试之后

> 写于 2026-09-17。上一份 `项目对比分析-iFace-vs-SelfFace.md` 写在三大功能落地**之前**，
> 结论已经过期。本文所有数字都是重新实测的，不是复述。

## 一句话结论

**功能上已经反超，内容上还差一个数量级。**

上一轮对比的结论是「互补而非竞争」—— iFace 胜在内容厚度与功能宽度，prep 胜在服务端工程。
现在这个判断需要修正：**prep 在「学习闭环」这条主线上已经完整超过 iFace**，
iFace 剩下的优势集中在题库规模（10.9 倍）和线上运行状态这两点。

---

## 实测指标对照

| 维度 | iFace | SelfFace | 差距 |
|---|---|---|---|
| 代码规模 | 35,614 行（48 文件 TS/TSX） | **10,695 行**（后端 main 5,290 + test 1,648 + 前端 3,757） | iFace 3.3× |
| 最大单文件 | **6,689 行**（QuestionDetail.tsx） | 630 行（Resume.vue） | prep 更干净 |
| 题库 | **1,222 题** / 29 模块 | 112 题 / 11 分类 | iFace **10.9×** |
| 功能页 | 11 个 | 8 个（+ 登录 / 布局） | 接近 |
| 服务端接口 | — （纯前端，IndexedDB） | **30 个** REST 接口 | prep 独有 |
| 数据表 | IndexedDB（无服务端） | **10 张** MySQL 表 | prep 独有 |
| 单元测试 | **0** | **78 个** | prep 独有 |
| 端到端验证脚本 | 15 个 check 脚本 | 7 个（smoke + 6 verify） | 各有侧重 |
| 部署状态 | **Vercel 线上，有真实用户** | Docker/compose 已写，**未实机跑过** | iFace 领先 |
| 多用户 | 无（纯本地） | 有（JWT + 数据隔离） | prep 独有 |

---

## 核心差异：学习闭环

这是加完功能后最有价值的一张对比。备考产品的价值不在「能刷题」，在于**能不能形成一个自动运转的闭环**：

```
刷题 → 记录 → 复习调度 → 暴露短板 → 定向强化 → 回到刷题
```

| 环节 | iFace | SelfFace |
|---|---|---|
| 刷题 | 有（练习 session） | 有（每日题单） |
| 记录 | 有（StudyRecord） | 有（practice_record 流水） |
| **复习调度** | **手动清单** | **SM-2 排期** |
| 短板暴露 | 有（WeakPoints） | 有（模拟面试打分） |
| **定向强化** | **止于报告** | **直接出题单** |

### 关于「复习调度」——这是最大的分水岭，有代码证据

查了 iFace 的 `src/store/useStudyStore.ts`，它的复习逻辑就这么多：

```js
const getWeakQuestions = useCallback((): StudyRecord[] => {
  return Object.values(stateRef.current.records)
    .filter((r) => r.status === 'review')              // 只看被标记成 review 的
    .sort((a, b) => a.lastUpdated - b.lastUpdated)     // 按最久没碰的排前面
}, [])
```

**这是筛选 + 排序，是「清单」，不是「排期」。** 它有三个特征：

1. **没有时间维度** —— 不存在「什么时候该复习」，只有「哪些题状态是待复习」。
2. **靠用户手动标记** —— 题进不进这个清单，取决于用户自己把状态改成 `review`。
3. **没有间隔推进** —— 复习一次就是复习一次，不记录「这次该隔多久再来」。

另外我逐词搜过它的代码库，**间隔重复相关的字段全部零命中**：

| 关键词 | 命中 | 说明 |
|---|---|---|
| `next_review` / `nextReview` | 0 | 没有下次复习时间 |
| `lastReviewed` | 0 | 没有上次复习时间 |
| `dueDate` / `due_date` | 0 | 没有到期概念 |
| `sm2` / `fsrs` / `srs` / `spaced` | 0 | 没有任何 SRS 算法 |
| `interval` | 1 | 唯一命中是 PromptPage 里讲事件循环的文案（`setInterval`） |
| `ease` | 10 | 全部是 CSS 缓动函数（`cubic-bezier` / `--ease-out`） |

**而 prep 现在做的是真排期**：每道题有 `next_review_at`，到期才进题单；间隔按自评推进
（连续答「掌握」走 **1 → 3 → 8 → 22 → 60 天**）；难度系数 EF 按表现动态调（夹在 `[1.30, 2.80]`）。
用户不需要手动挑题 —— **系统主动在"快忘的时候"把题推回来**。

### 关于「定向强化」

- **iFace 的 JdMatch**：贴 JD → 出匹配分 + 缺失关键词。**止于一份报告**，
  因为它题库是静态 JSON，没有服务端关联，出不了题单。
- **prep 的 JD 定向题单**：贴 JD → 抽关键词 → 题库召回 → 覆盖率 + 缺失技能 → **一键生成今日题单**。
  报告只是中间产物，落点是可执行的题目。

---

## 工程质量：两种不同的好

**iFace 的好是「纪律」**：Biome 强制格式化、15 个 check 脚本、11 项发布门禁、每个功能配文档。
这套流程比不少商业项目都齐。**但它有 0 个单元测试** —— 所有检查都在"构建产物 / 资源 / 发布条件"层面，
没有任何一个测试在验证**业务逻辑对不对**。

**prep 的好是「可验证」**：78 个单测（调度器间隔推进、EF 上下限、组卷规则、加密往返、限流阈值、
面试幂等与越权）+ 7 个端到端验证脚本（真跑 HTTP，含 AI 全链路）。**每一步都有可复核的产物**。

各有短板：
- iFace：逻辑正确性无测试保护，6,689 行的巨石文件改一处要靠人肉回归。
- prep：Docker/compose 只做了 `docker-compose config` 配置校验，**没有在真机跑起来过** ——
  这一项要如实标记为"未验证"，不能算已完成。

---

## 各自仍然独有的能力

| 只有 iFace 有 | 怎么看 |
|---|---|
| **1,222 题题库** | 这是它真正的护城河，也是 prep 现在最大的短板 |
| Prompt 管理页（2,461 行） | **不建议抄** —— 让用户手搓 prompt 是把质量责任推给用户 |
| AI 工具箱（719 行） | **不建议抄** —— 泛聊天框稀释产品焦点 |
| Vercel 线上运行 + 真实用户 | 值得学的是"有部署出口"，不是具体平台 |

| 只有 prep 有 | 价值 |
|---|---|
| **SM-2 间隔重复排期** | 算法护城河，iFace 完全没有 |
| 简历分析 Agent（两阶段 + 异步轮询 + 启动恢复） | 全栈链路能力 |
| 模拟面试闭环（多轮追问 + 打分 + 关联题库） | 解决「掌握度靠自评不客观」 |
| 30 个 REST 接口 / 10 张表 / 多用户隔离 | 服务端工程能力（简历要的正是这个） |
| 78 单测 + 7 个端到端验证 | 可验证性 |
| API Key AES-256-GCM 加密、限流、防爆破 | 生产意识 |

---

## 判断

**如果是"哪个更好的备考产品"**：现在 prep 更好。它的闭环是自动运转的，iFace 的复习要用户自己管。
但 iFace 有 1,222 道题，prep 只有 112 道 —— **用户在 prep 上刷两周就没题了**，
这是个会直接杀死产品的问题。

**如果是"哪个是更好的简历项目"**：prep 明显更强。它证明了服务端分层、异步任务、算法设计、
数据库建模、安全加固、测试覆盖 —— 这些是面试官会追问的；iFace 证明的是前端工程纪律和产品完成度，
但 0 单测 + 6,689 行巨石文件在技术面上是减分项。

**一句话**：功能层面的仗已经打完了，接下来是内容的仗。

---

## 下一步：重点应该转向内容厚度

功能再强，112 道题也撑不起一个"备考平台"。而现在**加题的通道已经打开了**：

1. **批量导入题库**（`POST /api/questions/import` + 前端导入页已完成）——
   可以去抓开源八股题库，转成项目格式批量灌进去。目标先到 300-500 题。
2. **题库要分方向**：现在 11 个分类全是 Java 后端。iFace 的 1222 题里 frontend 465、
   ai-agent 280、golang 175 —— 多方向才撑得起"平台"的定位。
3. **给导入的题做质量分层**：题多了要靠 `hot` 字段和掌握度数据做筛选，
   否则新用户面对 500 道题只会懵。

功能侧还剩的低成本补丁（优先级低于内容）：题单容量可配、题目收藏与笔记、连续打卡 streak、错题本导出。

---

## 附：本次实测的原始数据

```bash
# SelfFace
后端 main    66 文件 /  5,290 行
后端 test    10 文件 /  1,648 行
前端         16 文件 /  3,757 行
合计                 10,695 行
接口 30 个 | 表 10 张 | 题库 112 题 / 11 分类 | 单测 78 | 验证脚本 7 | 门禁脚本 2

# iFace
TS/TSX       48 文件 / 35,614 行
页面 11 个 | 题库 1,222 题 | 单测 0 | check 脚本 15
```
