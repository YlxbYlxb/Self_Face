# 生产构建的变量样例。复制成 .env.production 后按实际填写（该文件已被 .gitignore 忽略）。
#   cp .env.production.example .env.production
#
# 这些值在 npm run build / docker build 时被内联进产物，改了必须重新构建才生效。

# ICP 备案号，备案通过后由工信部/云厂商下发，形如「京ICP备2026000000号-1」。
# 留空则页面底部不渲染备案信息；填了就按工信部要求展示并链接到备案管理系统。
VITE_ICP_BEIAN=

# 备案号链接地址，一般不用改（工信部备案管理系统）
# VITE_ICP_BEIAN_URL=https://beian.miit.gov.cn/
